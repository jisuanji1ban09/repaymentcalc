<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itwzh.repaymentcalc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RepaymentCalc">
        <activity
            android:name=".activity.RepaymentPlanActivity"
            android:label="@string/repaymentPlan"
            android:exported="false" />
        <activity
            android:name=".activity.RepaymentCalcActivity"
            android:exported="false"
            android:label="@string/repaymentCalc" />
        <activity
            android:name=".activity.LoanCalcActivity"
            android:exported="false"
            android:label="@string/loanCalc" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:label="@string/appName">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>